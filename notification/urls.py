# notification/urls.py
# ==========================================

"""
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views

router = DefaultRouter()
router.register('', views.NotificationViewSet, basename='notification')
router.register('promo-codes', views.PromoCodeViewSet, basename='promocode')
router.register('settings', views.SiteSettingsViewSet, basename='settings')

app_name = 'notification'

urlpatterns = [
    path('', include(router.urls)),
]
"""