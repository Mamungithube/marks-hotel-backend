# room_management/urls.py
# ==========================================

"""
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views

router = DefaultRouter()
router.register('types', views.RoomTypeViewSet, basename='roomtype')
router.register('rooms', views.RoomViewSet, basename='room')
router.register('amenities', views.AmenityViewSet, basename='amenity')
router.register('images', views.RoomImageViewSet, basename='roomimage')

app_name = 'room_management'

urlpatterns = [
    path('', include(router.urls)),
]
"""